export type CompleteData = Partial<{
  firstName: string;
  lastName: string;
  birthDay: string;
  country: string;
  city: string;
  postalCode: string;
  address: string;
  email: string;
  phoneNumber: string;
}>;
